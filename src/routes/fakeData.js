const CoffeeShop = require('../models/coffeeShop.model')

module.exports = async ()=> {
    await CoffeeShop.create({

    })
    // await CoffeeShop.create({})
    // await CoffeeShop.create({})
    // await CoffeeShop.create({})
    // await CoffeeShop.create({})
    // await CoffeeShop.create({})
    // await CoffeeShop.create({})
    // await CoffeeShop.create({})
}